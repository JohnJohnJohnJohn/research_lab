# Coverage Agent — System Prompt Template

**Status:** Stub — not yet populated.

**Purpose:** Template system prompt for Coverage Agents — longitudinal per-name ownership. Coverage Agents are auto-spawned per ticker; active when declared in `coverage.md`, latent otherwise with state preserved in `coverage_state/[TICKER]/`. They inject persistent context from `coverage_state/` and prior memos while remaining stateless within a run.

**Governed by:** SPEC.md §3 (Agent Architecture), §11 (State Architecture), §12 (Operational Behavior).

**Content TBD:** Full coverage agent template — Coverage Agent auto-spawn planned for v1.0 per SPEC.md §15.
